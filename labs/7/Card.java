
package lab7;

public class Card {
    // Store the rank and suit since they're a card's identifying features
    private int rank;
    private int suit;
    
    // Creates a Card object with the specified rank and suit
    public Card(int rank, int suit) {
        this.rank = rank;
        this.suit = suit;
    }
}
